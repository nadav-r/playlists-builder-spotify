{
  "name": "spotify-app-server",
  "version": "1.0.0",
  "description": "",
  "main": "spotify-app-server.js",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "spotify-web-api-js": "^1.2.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "12.4.0"
  }
}
